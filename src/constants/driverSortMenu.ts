import type { SortMenuProps } from '@/interfaces/Dropdown/SortMenuInterface';

const SortMenu: SortMenuProps[] = [
  { name: '이사 빠른 순', code: 'UpcomingMoveDate' },
  { name: '요청일 빠른 순', code: 'RecentRequest' },
];

export default SortMenu;
