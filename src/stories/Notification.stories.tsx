import Notification from '@/components/common/gnb/Notification';

export default {
  title: 'Components/Common/Notification',
  component: Notification,
};

const Template = () => <Notification />;

export const Default = Template.bind({});
