import AddressModal from "@/components/modal/AddressModal";

export default {
  title: "Components/Modals/AddressModal",
  component: AddressModal,
};

const Template = () => <AddressModal />;

export const Default = Template.bind({});
