import { RegionChips } from "@/components/chips/RegionChips";

export default {
  title: "Components/Chips/RegionChips",
  component: RegionChips,
};

const Template = () => <RegionChips />;

export const Default = Template.bind({});
